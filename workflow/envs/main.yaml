name: rindti_clean

channels:
  - conda-forge
  - bioconda
  - pytorch 
  - nvidia

dependencies:
  # --- Core ---
  - python=3.9 
  - snakemake #done
  - snakemake-minimal 
  - pyyaml #already satisfied

  # --- PyTorch + GPU ---
  - pytorch=2.4 #1 done
  - pytorch-cuda=12.4 #already satisfied
  - torchvision #done
  - torchaudio #done

  # --- Scientific Python ---
  - numpy #already satisfied
  - pandas #4
  - scipy #done
  - scikit-learn #done
  - statsmodels #done

  # --- Plotting ---
  - matplotlib #done
  - seaborn #already satisfied

  # --- Chemistry / ML utils ---
  - rdkit #done
  - pytorch-lightning #3 done
  - tensorboard #done
  - torchmetrics #done
  - xgboost #done

  # --- Utility ---
  - requests #already satisfied
  - tqdm #already satisfied
  - joblib #already satisfied

  - pip:
      - torch-geometric #2 done
      - glycowork # exclude
      - gdown # exclude
