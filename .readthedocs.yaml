version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    post_create_environment:
      - pip install -e .[docs]

sphinx:
  configuration: docs/source/conf.py
  fail_on_warning: false
