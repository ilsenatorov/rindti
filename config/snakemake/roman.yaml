# the two root directories to take the data from and to store the data in
source: /scratch/SCRATCH_SAS/roman/rindti/datasets/oracle_test/resources
prots:
  features:
    method: esm # rinerator, distance, esm
drugs:
  node_feats: IUPAC # IUPAC, label, onehot, glycan
split_data:
  method: custom # custom, random, drug, target
  mode: prot
  train: 0.7
  val: 0.2
parse_dataset:
  filtering: all
  threshold: 1.645
sequence_only:
  drugs: True
