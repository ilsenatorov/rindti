# BindingDB restricted to a single measurement type:
# Kd, the dissociation constant. Directly comparable to Davis.
source: datasets/bindingdb_kd/resources
prots:
  structs:
    method: whole # whole, template, bsite, plddt
parse_dataset:
  unit: nM
  threshold: 100
